name: Hello World
on:
  workflow_run:
    workflows: Dependency Setup
    types: completed


jobs:
  hello-job:
    name: Preflight Step
    runs-on: ubuntu-latest
    steps:
      - run: poetry run python hello.py